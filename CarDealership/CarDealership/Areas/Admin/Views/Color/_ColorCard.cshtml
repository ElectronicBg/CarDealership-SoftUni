@model CarDealership.Data.CarColor

<div class="col-md-3 mb-3">
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">@Model.Name</h5>
            <div class="color-display mb-2 rounded-3" style="width:20px; height:20px; background-color: @Model.Value;"></div>
            <a href="@Url.Action("Edit", new { id = Model.CarColorId })" class="btn btn-primary">Редактирай</a>
        </div>
    </div>
</div>
